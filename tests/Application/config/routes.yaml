hraph_sylius_paygreen_plugin_shop:
    resource: "@SyliusPaygreenPlugin/Resources/config/shop_routing.yml"
    prefix: /{_locale}
    requirements:
        _locale: ^[a-z]{2}(?:_[A-Z]{2})?$

hraph_sylius_paygreen_plugin_admin:
    resource: "@SyliusPaygreenPlugin/Resources/config/admin_routing.yml"
    prefix: /admin
